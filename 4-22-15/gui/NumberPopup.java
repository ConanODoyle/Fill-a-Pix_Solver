package gui;

import javax.swing.Jpanel;
import javax.swing.Jbutton;

public class NumberPopup
{
  JFrame mainFrame = new JFrame("Pick one!");
  JPanel content = new JPanel()
  NumButton number = new NumButton(1);
  int posX;
  int posY;
  
  public NumberPopup(int x, int y)
  {
    for(int i=0;i<10;i++)
    {
    }
    number.addMouseListener(New MouseListener())
    {
    }
  }
}
